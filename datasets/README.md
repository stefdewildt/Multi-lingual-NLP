# Datasets

This folder will hold code and files related to the datasets we use in our research.

## MULTITuDE

`MULTITuDE` holds the dataset we will probably be using, note that this is a bigger dataset so you will have to download `multitude.csv` yourself, and don't add it to the repo (`.gitignore` it).

## FollowupQG (unused)

`FollowupQG` hold the smaller dataset we will probably not use. Files are stored directly in the git repo.
